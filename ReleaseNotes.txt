Jacaranda Comments Advanced 01.03.00
- Moves Advanced configuration to one portal-wide Comments Administration panel.
- Removes page-level editing of Jacaranda-specific settings in the Advanced branch while preserving normal DNN module title, container, visibility, and permission settings.
- Adds a one-time CentralSettingsActive migration flag so upgrading 01.02.x sites continue using their existing local module settings until a DNN Administrator or Superuser explicitly activates central management.
- Keeps the existing portal emergency switches active during migration so all posting or guest posting can still be disabled immediately.
- After activation, every Advanced module instance uses the portal settings for guest access, moderation, language filtering, comment length, rate limiting, CAPTCHA, and email notifications.
- Retains old local Jacaranda module settings in DNN for migration/recovery safety but ignores them after activation.
- Keeps central moderation, guest five-minute correction, registered-user 15-minute editing, approval/deletion, output encoding, anti-CSRF checks, parameterised SQL, and portal isolation unchanged.
- Adds CentralSettingsActive to JacarandaCommentsPortalSettings. Existing comments and replies are not modified.

Jacaranda Comments Advanced 01.02.02
- Adds a secure five-minute correction window for newly submitted guest comments and replies while they remain pending moderation.
- Allows guests to correct only the comment/reply text; guest display name and private email remain locked.
- Keeps the five-minute window anchored to the original CreatedOnDate so editing never restarts or extends the timer.
- Ends guest editing immediately when the submission is approved, deleted, the portal guest-posting switch is disabled, all posting is disabled, or the five-minute window expires; approval/deletion also clears the stored token hash.
- Generates a cryptographically strong server-session guest edit credential and stores only its SHA-256 hash with the guest submission.
- Never exposes the raw guest edit credential in the database, URL, hidden fields, rendered HTML, email, or public output.
- Does not use guest name, email address, IP address, user-agent text, CommentId alone, or the guest rate-limit key as proof of edit ownership.
- Repeats portal, page, module, ownership-token, pending-status, deletion-status, and five-minute checks in the parameterised SQL UPDATE.
- Shows a guest only their own recent pending submission while the correction credential remains valid, allowing the existing module Edit action to be used safely; Reply remains unavailable until a pending submission is approved.
- Re-evaluates the private language filter after a guest correction and keeps every guest correction pending for moderator approval.
- Adds GuestEditTokenHash NVARCHAR(64) NULL; existing guest comments remain non-editable because they have no token hash.
- Keeps the registered-user 15-minute edit window, portal-wide moderation queue, central settings, approval/deletion permissions, CAPTCHA, rate limiting, output encoding, and notification behaviour intact.

Jacaranda Comments Advanced 01.02.01
- Adds a portal-wide Pending Comments moderation queue to the Advanced administration panel.
- Shows unapproved comments and replies from every Jacaranda Comments module in the current portal, newest first, with 25 items per page.
- Shows the DNN page title, module title, comment/reply type, comment ID, author type, submission date, full safely encoded text, and private language-filter marker.
- Adds one-at-a-time Approve, Reject / Delete, and View Page actions; no bulk moderation actions are included.
- Restricts central moderation to DNN superusers and members of the current portal's built-in Administrators role.
- Repeats the portal administrator/superuser permission check and anti-CSRF security-token validation before every central moderation write.
- Scopes central approval and deletion by the server-derived PortalId and uses parameterised SQL.
- Reject / Delete soft-deletes the selected submission and any descendant replies within the same portal.
- View Page opens the originating DNN page and targets the module-scoped comment anchor when available.
- Renames the central navigation label to Comments Administration to reflect both moderation and site-wide settings.
- Adds a portal-moderation database index for efficient pending-queue queries; no comment records or module settings are changed.

Jacaranda Comments Advanced 01.02.00
- Starts the Advanced release line from the stable 01.01.03 code base; the stable Simple edition remains on the main branch at 01.01.03.
- Keeps the existing DNN package identity and installation folder, so Advanced is an upgrade path rather than a second side-by-side module.
- Adds one portal-scoped Site-wide Jacaranda Comments Settings panel, reachable from each module instance by DNN superusers and members of the portal's built-in Administrators role.
- Creates no new DNN security roles and repeats the administrator/superuser permission check on every central-panel request and save.
- Adds portal-wide emergency switches for all new posting and for guest posting; existing comments remain visible and moderation remains available when posting is disabled.
- Adds portal defaults for guest posting, moderation, language filtering, comment length, rate limiting, CAPTCHA, and notification behaviour.
- Adds a per-module “Use site-wide defaults” option that is switched off by default, preserving every existing module's local settings after upgrade.
- Keeps local values stored while inheritance is enabled so switching back to local settings restores the previous configuration.
- Scopes all central reads and writes by server-derived PortalId and uses parameterised SQL.
- Adds anti-CSRF validation, server-side value limits, administrator confirmation for portal-wide saves, and audit fields recording the UTC change time and DNN user ID.
- Shows administrators whether a module is inherited or local and displays the current portal emergency-control state.
- Adds the JacarandaCommentsPortalSettings database table; existing comments and module settings are not modified.

Jacaranda Comments 01.01.03
- Uses the current DNN page title in moderator notification email subjects.
- Uses the exact pending subject format “Comment awaiting approval — [Page title]” for comments and replies requiring moderation.
- Uses “New comment — [Page title]” for immediately approved new submissions and “Comment edited — [Page title]” for approved edits.
- Adds separate Page title and Page link lines to notification email bodies while retaining the portal, module, comment ID, author, status, language-filter flag, and submitted text details.
- Sanitises and length-limits the final email subject through the existing header-cleaning path.
- Improves CAPTCHA answer-field visibility with a three-pixel high-contrast border, larger bold text, increased input height, and a strong keyboard-focus outline.
- Replaces the previous starter notice with the complete MIT License so DNN displays the licence terms during installation.
- Attributes the MIT licence to Trevor Forrester and Forrest It Services.
- Keeps posting, replies, guest moderation, registered-author editing, language filtering, permissions, SQL parameterisation, output encoding, and notification delivery behaviour unchanged.
- No database schema changes.

Jacaranda Comments 01.01.02
- Adds an optional private language filter configured separately for each module instance.
- Keeps the filter switched off by default after new installations and upgrades.
- Lets authorised module editors enter one term or phrase per line on the settings screen; the list is not rendered or sent to public visitors.
- Performs case-insensitive whole-word and phrase matching after safe Unicode normalisation and punctuation separation.
- Never deletes, rewrites, or publicly identifies the matched text; a match quietly forces the unchanged submission into moderation.
- Applies the same rule to guest comments, registered comments, replies, and registered-author edits.
- Prevents editors and other normally auto-approved posters from bypassing a configured match; a moderator must review and approve it explicitly.
- Adds a moderator-only “Language filter” marker and a private notification-email note without revealing the configured term to the author.
- Limits the stored setting to 250 unique entries and 100 characters per entry, without compiling administrator input as executable regular expressions or SQL.
- Adds the IsLanguageFlagged database field for moderation/audit visibility; existing comments default to not flagged.

Jacaranda Comments 01.01.01
- Adds a per-module “Allow guest comments” setting, switched off by default.
- Allows signed-out visitors to submit comments and replies using a required display name and private email address.
- Holds every guest submission for moderator approval regardless of the registered-user moderation setting.
- Encrypts guest email addresses with ASP.NET MachineKey protection before database storage and never renders them publicly.
- Includes guest email only in the existing private moderator notification email.
- Uses a one-way SHA-256 guest rate-limit key based on the current network address and browser signature; the raw address is not stored.
- Keeps CAPTCHA, honeypot, anti-CSRF token validation, parameterised SQL, length validation, output encoding, and module/page scope checks in the guest workflow.
- Adds a server-enforced 15-minute edit window for registered authors to edit their own comments and replies.
- Does not allow guest editing; the guest form explains that registration or sign-in is required for editing.
- Returns non-editor edits to pending approval when moderation is required.
- Shows registered users their own pending comments so they can edit them during the allowed window.
- Records dedicated edit audit fields without confusing edits with moderator approval or deletion actions.
- Adds database columns GuestEmailEncrypted, GuestRateLimitKey, EditedOnDate, and EditedByUserId plus a guest rate-limit index.

Jacaranda Comments 01.00.20
- Positions success and posting-error notifications within the visible area of the Jacaranda Comments module that generated them.
- Keeps notifications inside the module column so they do not cover neighbouring article or content columns on multi-pane DNN pages.
- Recalculates notification position while scrolling or resizing, including long side-by-side article/comment layouts.
- Adds a strong four-pixel success/error border, enhanced shadow, status icon, and larger keyboard-visible close control.
- Preserves the existing 12-second automatic dismissal, direct close fallback, inline message fallback, posting, moderation, and exact return-to-item behaviour.
- No database schema changes.

Jacaranda Comments 01.00.19
- Binds and renders the current comment thread before registering post-completion targeting.
- Retries exact comment/reply targeting until the rendered nested item is available in the DNN page lifecycle.
- Extends settling passes so later DNN layout changes do not pull the page away from an immediately published reply.
- Keeps pending comments and replies at the confirmation area because they are not rendered for ordinary users.
- Keeps notifications, character limits, moderation, rate limiting, CAPTCHA, email, approval, and deletion behaviour unchanged.
- No database schema changes.

Jacaranda Comments 01.00.18
- Makes success and posting-error notifications dismiss reliably through both the close button and a fixed 12-second timeout.
- Removes indefinite hover-based timer cancellation.
- Positions notifications at the middle-right of the viewport, with a responsive centred-width layout on smaller screens.
- Gives every rendered comment and reply a unique module-scoped HTML target.
- Returns immediately approved comments and replies to their exact rendered item after the clean redirect.
- Validates the redirected comment ID against the current portal, page, module, approval, and deletion scope before using it.
- Keeps pending comments and replies at the confirmation area because they are not yet rendered publicly.
- No database schema changes.

Jacaranda Comments 01.00.17
- Keeps users at the comment form when a comment or reply fails length, CAPTCHA, or rate-limit validation.
- Shows posting validation errors in a fixed, dismissible notification so the reason remains visible in long comment threads.
- Positions success and error notifications at the middle-right of the viewport to reduce eye movement, with a centred responsive layout on small screens.
- Preserves the full comment text and places keyboard focus back in the comment box for correction.
- Stops the repeated scroll-to-top behaviour that could appear as a brief page flash after validation errors.
- No database schema changes.

Jacaranda Comments 01.00.16
- Stops browsers from silently truncating pasted comments at the configured character limit.
- Keeps the full pasted text in the comment box and shows the live counter as characters over the limit.
- Retains server-side length validation so over-limit submissions are rejected before database insertion or email notification.
- Adds a fixed success notification that remains visible wherever the user is positioned in a long comment thread.
- Keeps the existing inline completion message as a visual fallback and allows the popup notification to be dismissed or to close automatically.
- No database schema changes.

Jacaranda Comments 01.00.15
- Adds a per-module setting for the maximum length of comments and replies.
- Defaults to 4,000 characters and allows authorised module editors to choose a value from 250 to 10,000.
- Applies the saved limit consistently to the textarea, live remaining-character counter, server-side validation, error message, and parameterised SQL insert.
- Keeps over-length text in the form so the user can shorten it.
- Uses the current module setting dynamically in all guidance and validation messages.
- No database schema changes.

Jacaranda Comments 01.00.14
- Returns the page to the comment form when a registered user selects Reply.
- Places keyboard focus in the comment text box after reply selection.
- Uses repeated module-scoped scroll restoration so DNN page layout does not leave the user at the top after posting.
- Returns validation, security-token, CAPTCHA, and rate-limit messages to view instead of leaving them below the fold.
- Keeps posting, replies, moderation, rate limiting, CAPTCHA, notifications, and security checks unchanged.
- No database schema changes.

Jacaranda Comments 01.00.13
- Redirects successful approval and deletion actions to a clean GET response.
- Prevents browser form-resubmission prompts when refreshing after moderation.
- Shows approval/deletion confirmation once after the redirect and then removes the completion parameters from the URL.
- Keeps posting, replies, moderation permissions, rate limiting, CAPTCHA, notifications, and security-token behaviour unchanged.
- No database schema changes.

Jacaranda Comments 01.00.12
- Makes successful comment/reply completion messages one-time instead of recreating them from a persistent query string on every refresh.
- Requires the matching queued session message before accepting jcposted/jcmid completion parameters.
- Removes completion parameters from the browser URL after the first redirected load while retaining the comments-module anchor.
- Moves the redirect anchor onto the comments module container and defers focus/scroll until page load completes.
- Keeps posting, moderation, rate limiting, CAPTCHA, notifications, approval, deletion, and security-token behaviour unchanged.
- No database schema changes.

Jacaranda Comments 01.00.11
- Corrects the custom anti-CSRF/security-token lifecycle for comment, reply, approve, and delete postbacks.
- Initialises the hidden security token only on the initial GET request, preserving the browser-submitted value during postback validation.
- Keeps all existing posting, moderation, rate limiting, CAPTCHA, notification, and Post/Redirect/Get behaviour unchanged.
- No database schema changes.

Jacaranda Comments 01.00.10
- Changes successful comment/reply submission to a server-side redirect using Response.Redirect(..., false) plus CompleteRequest().
- Removes the previous dependence on the delayed client-side redirect for the normal success path.
- Keeps a fallback path that shows the message, clears fields, and rebinds comments if the server redirect cannot run.
- Clears queued session messages when the query-string completion message is used, avoiding duplicate completion messages later.
- No database schema changes.

Jacaranda Comments 01.00.09
- Fixes the success confirmation path so the user gets an immediate visible completion message after posting.
- Registers a client-side clean redirect back to the page with jcposted/jcmid status parameters.
- Reads the completion status from the query string after redirect instead of relying only on session state.
- Clears the comment, CAPTCHA, and honeypot fields on the clean redirected GET.
- Adds autocomplete="off" to the comment textarea to reduce browser restoration of old post text.
- No database schema changes.

Jacaranda Comments 01.00.08
- Adds a clearer “Process complete” confirmation after successful comment/reply submission.
- Redirects to the confirmation message anchor after the Post/Redirect/Get flow.
- Focuses and scrolls the confirmation message into view when possible.
- No database schema changes.

Jacaranda Comments 01.00.07
- Changes successful comment/reply posting to a Post/Redirect/Get flow.
- Prevents the browser from refilling the add-comment textarea after a manual page refresh.
- Carries the success/pending message through the redirect using a short-lived session value.
- No database schema changes.

01.00.06
- Clears the add-comment/reply text box after a successful post or pending submission.
- Also clears the CAPTCHA and hidden honeypot fields after successful form handling.
- Adds a small server-registered client-side cleanup script to catch browser/postback field retention.
- No database schema changes are required.

01.00.05
- Security audit/hardening release.
- Confirmed comment/reply SQL values use parameters rather than string-concatenated user input.
- Hardened SQL table identifier construction for DNN database owner/object qualifier values.
- Added a per-session security token check for comment, reply, approve, and delete postbacks.
- Locked stored display names to the logged-in DNN user profile to reduce impersonation/spoofing.
- Hardened email address validation and removed CR/LF from notification email subjects.

01.00.04
- Hotfix: fully qualified DotNetNuke.Common.Globals.NavigateURL in View.ascx.
- Fixes DNN runtime compile error CS0104 where 'Globals' was ambiguous between DotNetNuke.Common.Globals and Microsoft.VisualBasic.Globals.

Version 01.00.03

- Added a DNN module settings screen for moderation, anti-spam, and notification preferences.
- Added optional moderation setting to hold or auto-approve non-editor comments/replies.
- Added rate limiting for non-editor posters.
- Added optional built-in math CAPTCHA for non-editor posters.
- Added email notifications for new comments and replies, with configurable recipient addresses.
- Kept ownership metadata for Trevor Forrester / Forrest It Services.

Version 01.00.02

- Updated package/module details and ownership metadata.
- Owner: Trevor Forrester
- Organization: Forrest It Services
- URL: https://forrestitservices.org
- Email Address: webmaster@forrestitservices.org

Version 01.00.01

- Added threaded replies using ParentCommentId.
- Logged-in users can reply to an existing visible comment.
- Replies use the same approval/moderation rules as top-level comments.
- Reply display is indented up to three visual levels.
- Includes an upgrade SQL script for existing 01.00.00 installs.

Version 01.00.00

- Initial DNN 10 page comments module.
- Logged-in users can submit comments.
- Non-editor comments are held for approval.
- Page/module editors, administrators, and superusers can approve or delete comments.
