Jacaranda Comments 01.00.20
- Positions success and posting-error notifications within the visible area of the Jacaranda Comments module that generated them.
- Keeps notifications inside the module column so they do not cover neighbouring article or content columns on multi-pane DNN pages.
- Recalculates notification position while scrolling or resizing, including long side-by-side article/comment layouts.
- Adds a strong four-pixel success/error border, enhanced shadow, status icon, and larger keyboard-visible close control.
- Preserves the existing 12-second automatic dismissal, direct close fallback, inline message fallback, posting, moderation, and exact return-to-item behaviour.
- No database schema changes.

Jacaranda Comments 01.00.19
- Binds and renders the current comment thread before registering post-completion targeting.
- Retries exact comment/reply targeting until the rendered nested item is available in the DNN page lifecycle.
- Extends settling passes so later DNN layout changes do not pull the page away from an immediately published reply.
- Keeps pending comments and replies at the confirmation area because they are not rendered for ordinary users.
- Keeps notifications, character limits, moderation, rate limiting, CAPTCHA, email, approval, and deletion behaviour unchanged.
- No database schema changes.

Jacaranda Comments 01.00.18
- Makes success and posting-error notifications dismiss reliably through both the close button and a fixed 12-second timeout.
- Removes indefinite hover-based timer cancellation.
- Positions notifications at the middle-right of the viewport, with a responsive centred-width layout on smaller screens.
- Gives every rendered comment and reply a unique module-scoped HTML target.
- Returns immediately approved comments and replies to their exact rendered item after the clean redirect.
- Validates the redirected comment ID against the current portal, page, module, approval, and deletion scope before using it.
- Keeps pending comments and replies at the confirmation area because they are not yet rendered publicly.
- No database schema changes.

Jacaranda Comments 01.00.17
- Keeps users at the comment form when a comment or reply fails length, CAPTCHA, or rate-limit validation.
- Shows posting validation errors in a fixed, dismissible notification so the reason remains visible in long comment threads.
- Positions success and error notifications at the middle-right of the viewport to reduce eye movement, with a centred responsive layout on small screens.
- Preserves the full comment text and places keyboard focus back in the comment box for correction.
- Stops the repeated scroll-to-top behaviour that could appear as a brief page flash after validation errors.
- No database schema changes.

Jacaranda Comments 01.00.16
- Stops browsers from silently truncating pasted comments at the configured character limit.
- Keeps the full pasted text in the comment box and shows the live counter as characters over the limit.
- Retains server-side length validation so over-limit submissions are rejected before database insertion or email notification.
- Adds a fixed success notification that remains visible wherever the user is positioned in a long comment thread.
- Keeps the existing inline completion message as a visual fallback and allows the popup notification to be dismissed or to close automatically.
- No database schema changes.

Jacaranda Comments 01.00.15
- Adds a per-module setting for the maximum length of comments and replies.
- Defaults to 4,000 characters and allows authorised module editors to choose a value from 250 to 10,000.
- Applies the saved limit consistently to the textarea, live remaining-character counter, server-side validation, error message, and parameterised SQL insert.
- Keeps over-length text in the form so the user can shorten it.
- Uses the current module setting dynamically in all guidance and validation messages.
- No database schema changes.

Jacaranda Comments 01.00.14
- Returns the page to the comment form when a registered user selects Reply.
- Places keyboard focus in the comment text box after reply selection.
- Uses repeated module-scoped scroll restoration so DNN page layout does not leave the user at the top after posting.
- Returns validation, security-token, CAPTCHA, and rate-limit messages to view instead of leaving them below the fold.
- Keeps posting, replies, moderation, rate limiting, CAPTCHA, notifications, and security checks unchanged.
- No database schema changes.

Jacaranda Comments 01.00.13
- Redirects successful approval and deletion actions to a clean GET response.
- Prevents browser form-resubmission prompts when refreshing after moderation.
- Shows approval/deletion confirmation once after the redirect and then removes the completion parameters from the URL.
- Keeps posting, replies, moderation permissions, rate limiting, CAPTCHA, notifications, and security-token behaviour unchanged.
- No database schema changes.

Jacaranda Comments 01.00.12
- Makes successful comment/reply completion messages one-time instead of recreating them from a persistent query string on every refresh.
- Requires the matching queued session message before accepting jcposted/jcmid completion parameters.
- Removes completion parameters from the browser URL after the first redirected load while retaining the comments-module anchor.
- Moves the redirect anchor onto the comments module container and defers focus/scroll until page load completes.
- Keeps posting, moderation, rate limiting, CAPTCHA, notifications, approval, deletion, and security-token behaviour unchanged.
- No database schema changes.

Jacaranda Comments 01.00.11
- Corrects the custom anti-CSRF/security-token lifecycle for comment, reply, approve, and delete postbacks.
- Initialises the hidden security token only on the initial GET request, preserving the browser-submitted value during postback validation.
- Keeps all existing posting, moderation, rate limiting, CAPTCHA, notification, and Post/Redirect/Get behaviour unchanged.
- No database schema changes.

Jacaranda Comments 01.00.10
- Changes successful comment/reply submission to a server-side redirect using Response.Redirect(..., false) plus CompleteRequest().
- Removes the previous dependence on the delayed client-side redirect for the normal success path.
- Keeps a fallback path that shows the message, clears fields, and rebinds comments if the server redirect cannot run.
- Clears queued session messages when the query-string completion message is used, avoiding duplicate completion messages later.
- No database schema changes.

Jacaranda Comments 01.00.09
- Fixes the success confirmation path so the user gets an immediate visible completion message after posting.
- Registers a client-side clean redirect back to the page with jcposted/jcmid status parameters.
- Reads the completion status from the query string after redirect instead of relying only on session state.
- Clears the comment, CAPTCHA, and honeypot fields on the clean redirected GET.
- Adds autocomplete="off" to the comment textarea to reduce browser restoration of old post text.
- No database schema changes.

Jacaranda Comments 01.00.08
- Adds a clearer “Process complete” confirmation after successful comment/reply submission.
- Redirects to the confirmation message anchor after the Post/Redirect/Get flow.
- Focuses and scrolls the confirmation message into view when possible.
- No database schema changes.

Jacaranda Comments 01.00.07
- Changes successful comment/reply posting to a Post/Redirect/Get flow.
- Prevents the browser from refilling the add-comment textarea after a manual page refresh.
- Carries the success/pending message through the redirect using a short-lived session value.
- No database schema changes.

01.00.06
- Clears the add-comment/reply text box after a successful post or pending submission.
- Also clears the CAPTCHA and hidden honeypot fields after successful form handling.
- Adds a small server-registered client-side cleanup script to catch browser/postback field retention.
- No database schema changes are required.

01.00.05
- Security audit/hardening release.
- Confirmed comment/reply SQL values use parameters rather than string-concatenated user input.
- Hardened SQL table identifier construction for DNN database owner/object qualifier values.
- Added a per-session security token check for comment, reply, approve, and delete postbacks.
- Locked stored display names to the logged-in DNN user profile to reduce impersonation/spoofing.
- Hardened email address validation and removed CR/LF from notification email subjects.

01.00.04
- Hotfix: fully qualified DotNetNuke.Common.Globals.NavigateURL in View.ascx.
- Fixes DNN runtime compile error CS0104 where 'Globals' was ambiguous between DotNetNuke.Common.Globals and Microsoft.VisualBasic.Globals.

Version 01.00.03

- Added a DNN module settings screen for moderation, anti-spam, and notification preferences.
- Added optional moderation setting to hold or auto-approve non-editor comments/replies.
- Added rate limiting for non-editor posters.
- Added optional built-in math CAPTCHA for non-editor posters.
- Added email notifications for new comments and replies, with configurable recipient addresses.
- Kept ownership metadata for Trevor Forrester / Forrest It Services.

Version 01.00.02

- Updated package/module details and ownership metadata.
- Owner: Trevor Forrester
- Organization: Forrest It Services
- URL: https://forrestitservices.org
- Email Address: webmaster@forrestitservices.org

Version 01.00.01

- Added threaded replies using ParentCommentId.
- Logged-in users can reply to an existing visible comment.
- Replies use the same approval/moderation rules as top-level comments.
- Reply display is indented up to three visual levels.
- Includes an upgrade SQL script for existing 01.00.00 installs.

Version 01.00.00

- Initial DNN 10 page comments module.
- Logged-in users can submit comments.
- Non-editor comments are held for approval.
- Page/module editors, administrators, and superusers can approve or delete comments.
